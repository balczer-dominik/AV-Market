query AdPreview($adId: Int!) {
  ad(id: $adId) {
    ad {
      id
      title
      price
      createdAt
      updatedAt
      thumbnail
      owner {
        id
        username
        ...UserLocation
      }
    }
  }
}
