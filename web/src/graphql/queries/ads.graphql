query Ads(
  $first: Int
  $cursor: String
  $search: AdSearch!
  $sortBy: AdSortingOptions!
) {
  ads(first: $first, cursor: $cursor, search: $search, sortBy: $sortBy) {
    ads {
      ...AdSnippet
    }
    hasMore
  }
}
